# Default values for anime-api.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

deployment:
  name: recommender-api
  replicas: 1
  port: 5001
  labels: recommender-api
  selectorLabel: recommender-api
  image: registry.gitlab.com/cs302-2023/g2-team3/services/recommender/main:8f4d3bf74172baf5fa7ede4f62acb2c80daa54ad

service:
  name: recommender
  type: ClusterIP
  port: 80
  targetPort: 5001

ingress:
  name: recommender-ingress
  route: /recommendations
