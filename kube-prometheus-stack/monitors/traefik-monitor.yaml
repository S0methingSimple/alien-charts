apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: traefik-monitor
  labels:
    release: prometheus
spec:
  endpoints:
  - path: /metrics
    port: "9100"
  selector:
    matchLabels:
      app: traefik-888b685cd